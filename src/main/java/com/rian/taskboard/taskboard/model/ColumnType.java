package com.rian.taskboard.taskboard.model;

public enum ColumnType {
    INITIAL,    // coluna inicial
    FINAL,      // coluna final
    CANCEL,     // coluna de cancelamento
    PENDING     // colunas intermediárias (quantas quiser)
}
